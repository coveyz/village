

const getters = {
  token: (state: any) => state.user.token,
  // permission_list: (state: any) => state.user.permission_list,
  // permission_routes: (state: any) => state.permission.routes
}

export default getters